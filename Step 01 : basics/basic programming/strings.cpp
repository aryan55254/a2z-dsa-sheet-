#include <string>
using namespace std;

int main (){
     
    string s1 = "hello" ; 
    cin >> s2;//to input one word
    getline (cin , s2); // to input a line 
    //access
    s[0] //to acess
    s[1] = 'd' // to change 

    //functions 
    s.lenght(); //size of string 
    s.substr(0,3);// gets substring 
    //s.find // tell position 
    s + "";  //concat

    //character arrays = low level way of handling strings 
    char name[100];
    cin >> name; //wrks liek string input 
    

    return 0;
}